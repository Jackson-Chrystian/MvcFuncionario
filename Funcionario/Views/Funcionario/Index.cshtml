@model IEnumerable<Funcionario.Models.tblFuncionario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Nome
        </th>
        <th>
            Cargo
        </th>
        <th>
            Setor
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cargo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tlbSetor.nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_funcionario }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_funcionario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_funcionario })
        </td>
    </tr>
}

</table>
